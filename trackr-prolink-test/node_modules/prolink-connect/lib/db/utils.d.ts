import { Device, MediaSlot } from "../types";
declare type AnlzLoaderOpts = {
    device: Device;
    slot: MediaSlot.RB | MediaSlot.USB | MediaSlot.SD;
};
export declare function anlzLoader(opts: AnlzLoaderOpts): (path: string) => Promise<Buffer>;
export {};
